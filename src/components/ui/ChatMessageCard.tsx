import React from "react";
import { StyleSheet, Text, View } from "react-native";

const ChatMessageCard = ({ item, currentUser }) => {
  return (
    <View>
      <Text>ChatMessageCard</Text>
    </View>
  );
};

export default ChatMessageCard;

const styles = StyleSheet.create({});
